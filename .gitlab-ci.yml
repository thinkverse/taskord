image: alpine:latest

before_script:
  - /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
  - brew bundle

test:
  script:
    - vendor/bin/pest -d memory_limit=4G --coverage --coverage-text --colors=never
